import { atom } from 'recoil';

export const ListSelectCategory = atom({
    key: 'listSelectCategory',
    default: false,
});

export const ListSelectRegion = atom ({
    key: 'listSelectRegion',
    default: false,
});